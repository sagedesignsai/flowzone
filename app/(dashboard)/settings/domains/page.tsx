import { DomainsSection } from "@/components/settings/settings-sections/domains-section"

export const metadata = {
  title: "Domains — Settings — Flowzone",
}

export default function DomainsSettingsPage() {
  return <DomainsSection />
}
