import { GitHubSection } from "@/components/settings/settings-sections/github-section"

export const metadata = {
  title: "GitHub — Settings — Flowzone",
}

export default function GitHubSettingsPage() {
  return <GitHubSection />
}
